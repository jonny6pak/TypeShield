#!/bin/bash
set -euo pipefail

PLIST="$HOME/Library/LaunchAgents/com.typeshield.agent.plist"
UID_NUM="$(id -u)"

launchctl bootout "gui/$UID_NUM" "$PLIST" >/dev/null 2>&1 || true
rm -f "$PLIST"

echo "Removed TypeShield LaunchAgent. Binary is unchanged."
